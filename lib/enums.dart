enum Screens{
  fileManager,
  packageManager,
  other
}

enum FileUploadType{
  file,
  directory
}

enum FileContentTypes{
  file,
  directory,
  pdf,
  document,
  image,
  audio,
  video,
  archive,
  apk,
  torrent,
  securityCertificate,
}

enum FileTransferType{
  move,
  copy,
  interDevice
}

enum UpdateStatus{
  latest,
  available
}