enum Screens{
  fileManager,
  packageManager,
  powerControls,
  other
}

enum FileItemType{
  file,
  directory
}

enum FileContentTypes{
  file,
  directory,
  pdf,
  wordDocument,
  powerpoint,
  excel,
  image,
  audio,
  video,
  archive,
  apk,
  torrent,
  securityCertificate,
}

enum FileTransferType{
  move,
  copy,
  pcToPhone,
  phoneToPC
}

enum AppType{
  system,
  user
}