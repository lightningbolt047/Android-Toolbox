enum Screens{
  fileManager,
  packageManager,
  powerControls,
  other
}

enum FileUploadType{
  file,
  directory
}

enum FileContentTypes{
  file,
  directory,
  pdf,
  wordDocument,
  powerpoint,
  excel,
  image,
  audio,
  video,
  archive,
  apk,
  torrent,
  securityCertificate,
}

enum FileTransferType{
  move,
  copy,
  interDevice
}